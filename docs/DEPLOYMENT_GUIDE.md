# Cloudflare 一鍵部署功能說明

## 🚀 功能概述

新增了簡化的一鍵部署功能，讓操作者可以通過 Admin 面板直接將活動設定推送到 GitHub，觸發 Cloudflare Pages 自動部署。

---

## 📋 使用步驟

### 1. 儲存活動設定
1. 在 Admin 面板點擊「⚙️ 設定」
2. 編輯活動頁面設定（標題、副標題、字型、背景圖片等）
3. 點擊「💾 儲存活動設定」

### 2. 檢查部署狀態
- 設定頁面會自動顯示部署狀態區塊
- 狀態標籤說明：
  - 🟡 **有變更**：本地設定已更改，尚未提交
  - 🟠 **待推送**：已提交但尚未推送到 GitHub
  - 🟢 **已同步**：已推送到 GitHub，與遠端同步

### 3. 執行部署
1. 點擊「🚀 部署到 Cloudflare」按鈕
2. 確認部署對話框：
   - 確認要執行的操作
   - 了解部署流程
3. 點擊「確定」開始部署

### 4. 等待部署完成
- 部署過程通常需要 10-30 秒
- Cloudflare Pages 自動部署需要 1-3 分鐘
- 部署成功後會顯示成功訊息

---

## ⚠️ 重要注意事項

### Git 認證設定

部署功能需要 Git 認證。請確保已設定以下其中一種：

#### 方式 1: SSH Key（推薦）
```bash
# 檢查是否已有 SSH key
ls -la ~/.ssh/id_rsa.pub

# 如果沒有，生成新的 SSH key
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# 將公鑰添加到 GitHub
cat ~/.ssh/id_rsa.pub
# 複製輸出內容，到 GitHub Settings > SSH and GPG keys > New SSH key
```

#### 方式 2: Personal Access Token
1. 到 GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
2. 生成新 token，勾選 `repo` 權限
3. 使用 token 作為密碼：
```bash
git config --global credential.helper store
# 下次 push 時輸入用戶名和 token
```

### GitHub 倉庫路徑

預設 GitHub 倉庫路徑為：
```
/Users/nomisas/Documents/GitHub/live-event-photography
```

如果路徑不同，需要修改 `server.py` 中的 `GITHUB_REPO_PATH` 變數。

---

## 🔧 部署流程詳解

當點擊「部署到 Cloudflare」時，系統會執行以下步驟：

1. **複製設定文件**
   - 將 `event_settings.json` 複製到 GitHub 倉庫

2. **Git 操作**
   - `git add event_settings.json` - 暫存變更
   - `git commit -m "Update event settings"` - 提交變更
   - `git push` - 推送到 GitHub

3. **Cloudflare 自動部署**
   - GitHub 收到 push 後，Cloudflare Pages 會自動觸發部署
   - 部署通常需要 1-3 分鐘完成

---

## ❌ 常見錯誤處理

### 錯誤 1: "GitHub repository not found"
**原因**：GitHub 倉庫路徑不正確或不存在

**解決方案**：
- 確認倉庫路徑是否正確
- 檢查 `server.py` 中的 `GITHUB_REPO_PATH` 設定

### 錯誤 2: "Git 認證失敗"
**原因**：未設定 SSH key 或 Personal Access Token

**解決方案**：
- 按照上述「Git 認證設定」步驟設定認證
- 測試認證：`git -C /Users/nomisas/Documents/GitHub/live-event-photography push`

### 錯誤 3: "nothing to commit"
**原因**：設定沒有變更，或已經是最新的

**解決方案**：
- 這是正常情況，表示設定已經是最新的
- 可以繼續使用，無需重新部署

### 錯誤 4: "Git 操作超時"
**原因**：網路連接問題或 Git 操作時間過長

**解決方案**：
- 檢查網路連接
- 重試部署
- 如果持續失敗，可以手動執行：
```bash
cd /Users/nomisas/Documents/GitHub/live-event-photography
git add event_settings.json
git commit -m "Update event settings"
git push
```

---

## 📊 部署狀態檢查

### 在 Admin 面板檢查
- 打開設定頁面，查看「🌐 Cloudflare 部署」區塊
- 狀態標籤會顯示當前狀態

### 手動檢查
```bash
cd /Users/nomisas/Documents/GitHub/live-event-photography
git status
git log -1
```

### 檢查 Cloudflare 部署
1. 登入 Cloudflare Dashboard
2. 進入 Pages 專案
3. 查看部署歷史記錄

---

## 💡 最佳實踐

1. **先測試本地**：在本地活動頁面確認設定正確後再部署
2. **定期檢查**：部署後等待 1-3 分鐘，檢查 Cloudflare 活動頁面是否更新
3. **備份設定**：重要活動前，建議備份 `event_settings.json`
4. **一次部署**：避免頻繁部署，可以累積多個變更後一次部署

---

## 🔄 自動化建議

如果需要更自動化的部署，可以考慮：

1. **GitHub Actions**：設定自動部署工作流
2. **Webhook**：設定 Cloudflare Pages webhook 觸發部署
3. **定時同步**：設定定時任務自動同步設定

---

**更新時間**: 2026-01-23  
**狀態**: ✅ 功能已完成並可測試
